# Video assets

Place the showreel export here as `showreel.mp4`.

`src/components/Showreel/ShowreelVideo.jsx` already points at `/videos/showreel.mp4` — dropping the file in this folder is the only step needed to wire it up.
